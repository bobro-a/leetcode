#include <algorithm>
#include <vector>
#include <stack>
#include <set>
#include <iostream>
using namespace std;

class Solution {
public:
    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {

        for (int i=0;i<gas.size();++i)
        {

        }
    }
};


int main()
{
    vector<int> gas = {1,2,3,4,5};
    vector<int> cost={3,4,5,1,2};
    Solution a;
    int rez = a.canCompleteCircuit(gas,cost);
    return 0;
}
